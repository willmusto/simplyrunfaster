At the end of every session, or whenever asked to deploy: 1) commit all changes with an appropriate descriptive commit message, 2) push to main on GitHub, 3) SSH into gwmusto_simplyrunfaster@ssh.nyc1.nearlyfreespeech.net and run: cd /home/private/app && git pull && rsync -av --exclude='.git' --exclude='config/config.local.php' --exclude='scripts/' /home/private/app/ /home/public/ — This deploys the latest code to production at simplyrunfaster.com.
