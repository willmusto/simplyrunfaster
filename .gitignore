# Spec documents (not for deployment)
_specs/

# Claude Code instructions — kept locally, never tracked/deployed (contains deploy/SSH details)
CLAUDE.md

# Local config with production credentials
config/config.local.php

# PHP composer
vendor/
composer.lock

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
