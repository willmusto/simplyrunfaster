At the end of every session, or whenever asked to deploy: 1) bump the service worker cache version by running `sed -i "s/srf-[0-9]*/srf-$(date +%Y%m%d)/" sw.js` in the project root — do this every deploy, even for small changes, since any deploy could include CSS or view changes and without it users see stale CSS/views until a hard refresh, 2) commit all changes with an appropriate descriptive commit message, 3) push to main on GitHub, 4) SSH into gwmusto_simplyrunfaster@ssh.nyc1.nearlyfreespeech.net and run: cd /home/private/app && git pull && rsync -av --exclude='.git' --exclude='config/config.local.php' --exclude='scripts/' /home/private/app/ /home/public/ — This deploys the latest code to production at simplyrunfaster.com.
